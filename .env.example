# AWS Amplify Configuration
# These environment variables are required for AWS Amplify integration
# Copy this file to .env and fill in your actual values

# AWS Region where your resources are located
VITE_AWS_REGION=us-east-1

# S3 Bucket name for file storage
VITE_AWS_S3_BUCKET=your-bucket-name

# Cognito Identity Pool ID for authentication
VITE_AWS_IDENTITY_POOL_ID=us-east-1:your-identity-pool-id

# Cognito User Pool ID (optional, if using authentication)
VITE_AWS_USER_POOL_ID=us-east-1_your-user-pool-id

# Cognito User Pool Web Client ID (optional, if using authentication)
VITE_AWS_USER_POOL_WEB_CLIENT_ID=your-web-client-id

# GraphQL API endpoint (optional, if using AppSync)
VITE_AWS_GRAPHQL_ENDPOINT=https://your-graphql-endpoint.appsync-api.us-east-1.amazonaws.com/graphql

# Database connection (for production deployment)
DATABASE_URL=your-database-connection-string